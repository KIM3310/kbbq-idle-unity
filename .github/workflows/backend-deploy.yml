name: backend-deploy

on:
  push:
    branches: [main]
    paths:
      - "server/**"
      - "docker-compose.yml"
      - ".github/workflows/backend-deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4
      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ghcr.io/${{ github.repository_owner }}/kbbq-idle-unity-api
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}
      - uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: server/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-remote:
    runs-on: ubuntu-latest
    needs: [build-image]
    env:
      KBBQ_DEPLOY_HOST: ${{ secrets.KBBQ_DEPLOY_HOST }}
      KBBQ_DEPLOY_USER: ${{ secrets.KBBQ_DEPLOY_USER }}
      KBBQ_DEPLOY_SSH_KEY: ${{ secrets.KBBQ_DEPLOY_SSH_KEY }}
      KBBQ_GHCR_USERNAME: ${{ secrets.KBBQ_GHCR_USERNAME }}
      KBBQ_GHCR_PAT: ${{ secrets.KBBQ_GHCR_PAT }}
    steps:
      - name: Check deploy configuration
        id: config
        run: |
          set -euo pipefail
          if [[ "$GITHUB_REF" != "refs/heads/main" ]]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          missing=0
          for key in KBBQ_DEPLOY_HOST KBBQ_DEPLOY_USER KBBQ_DEPLOY_SSH_KEY KBBQ_GHCR_USERNAME KBBQ_GHCR_PAT; do
            if [[ -z "${!key}" ]]; then
              echo "missing secret: $key"
              missing=1
            fi
          done

          if [[ "$missing" -eq 1 ]]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: Deploy via SSH
        if: steps.config.outputs.enabled == 'true'
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ env.KBBQ_DEPLOY_HOST }}
          username: ${{ env.KBBQ_DEPLOY_USER }}
          key: ${{ env.KBBQ_DEPLOY_SSH_KEY }}
          script: |
            set -euo pipefail
            IMAGE="ghcr.io/${{ github.repository_owner }}/kbbq-idle-unity-api:latest"
            docker login ghcr.io -u "${{ env.KBBQ_GHCR_USERNAME }}" -p "${{ env.KBBQ_GHCR_PAT }}"
            docker pull "$IMAGE"
            docker rm -f kbbq-api || true
            docker run -d \
              --name kbbq-api \
              --restart unless-stopped \
              --env-file /opt/kbbq/.env \
              -p 8000:8000 \
              "$IMAGE"
